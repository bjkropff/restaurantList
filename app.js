var BestRest = angular.module("BestRest", []);


// <!-- ng-hide = editing -- editing does not exist, so it's false. 
// ng-hide will hide when editing is true, so span is initially showing     -->
// {{ element.name }} <a ng-click="deleteRestaurant(element)">Delete</a>
// </span>
// <form ng-show = "editing" ng-submit="editing = false">
// <!-- ng-show is initially hidden, it will show when editing is true -->
// <!-- we turn editing true when we click the first ng-hide -->
//     <input type = "text" ng-model ="element.name">
